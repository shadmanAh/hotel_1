.container
  .card 
    .card-header
      %h1 
        = @service.title
    .card-body
      = @service.description
    .card-footer
      = link_to 'Edit', edit_service_path(@service)
      \|
      = link_to 'Back', services_path
