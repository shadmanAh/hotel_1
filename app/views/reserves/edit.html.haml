- content_for :title do 
    Hotel 
    \|
    Editing 
    = @reserve.to_s

.container
    .card 
        .card-header
            %h1 Editing reserf
        .card-body
            = render 'form'
        .card-footer
            = link_to 'Show', @reserf
            \|
            - if current_user.has_role?(:admin)
                = link_to 'Back', reserves_path
            - else 
                = link_to 'Back', rooms_path
